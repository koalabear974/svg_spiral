{
  "name": "p5stuff",
  "version": "1.0.0",
  "description": "",
  "main": "sketch.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "osc": "nodemon server.js --watch server.js",
    "bs": "browser-sync start --server --directory --files * --startPath index.html",
    "interact": "browser-sync start --server --directory --files * --startPath interact.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/blob": "^2.4.0",
    "express": "^5.1.0",
    "osc": "^2.4.5",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
